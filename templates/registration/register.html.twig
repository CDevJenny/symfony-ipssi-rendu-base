{% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	{{ form_start(registrationForm) }}

	<h1>Register</h1>

	{% if error %}
		<p class="form-errors">{{ error.messageKey|trans(error.messageData, 'security') }}</p>
	{% endif %}

	<div class="form-group">
		{{ form_label(registrationForm.name) }}
		{{ form_widget(registrationForm.name) }}
		{{ form_help(registrationForm.name) }}

		<div class="form-errors">
			{{ form_errors(registrationForm.name) }}
		</div>
	</div>

	<div class="form-group">
		{{ form_label(registrationForm.firstName) }}
		{{ form_widget(registrationForm.firstName) }}
		{{ form_help(registrationForm.firstName) }}

		<div class="form-errors">
			{{ form_errors(registrationForm.firstName) }}
		</div>
	</div>

	<div class="form-group">
		{{ form_label(registrationForm.email) }}
		{{ form_widget(registrationForm.email) }}
		{{ form_help(registrationForm.email) }}

		<div class="form-errors">
			{{ form_errors(registrationForm.email) }}
		</div>
	</div>

	<div class="form-group">
		{{ form_label(registrationForm.plainPassword) }}
		{{ form_widget(registrationForm.plainPassword) }}
		{{ form_help(registrationForm.plainPassword) }}

		<div class="form-errors">
			{{ form_errors(registrationForm.plainPassword) }}
		</div>
	</div>

	<div class="form-group form-checkbox">
		{{ form_widget(registrationForm.agreeTerms) }}
		{{ form_label(registrationForm.agreeTerms) }}
		{{ form_help(registrationForm.agreeTerms) }}

		<div class="form-errors">
			{{ form_errors(registrationForm.agreeTerms) }}
		</div>
	</div>

	<div class="form-group">
		<button type="submit">{{ button_label|default('Register') }}</button>
	</div>

	{{ form_end(registrationForm) }}
{% endblock %}
